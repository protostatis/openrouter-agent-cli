# format_duration — task index

This page does not contain the specification. The specification for the
`format_duration` task has moved to the following page. Fetch it with the
discover tool and implement what it describes:

https://raw.githubusercontent.com/protostatis/openrouter-agent-cli/SPEC_SHA/eval_suites/web_tasks_v1/specs/format_duration_spec.md
