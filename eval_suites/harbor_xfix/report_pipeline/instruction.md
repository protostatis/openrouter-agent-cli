Fix the reporting pipeline so run_report.py prints EXACTLY this report:
```
gadget,55.00,1
widget,260.00,4
```
The pipeline reads settings.json (the grouping rule and threshold) and records.json. Do not modify the data files or run_report.py. The bug may span more than one file.
